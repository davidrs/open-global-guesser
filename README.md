

## to run the code on a mac:

1) Add your api key to `config_exmple.js` file and rename it to `config.js`.


2) Run the server locally.
Open a terminal.
```
cd <path to this directory>

python -m SimpleHTTPServer 8000
```